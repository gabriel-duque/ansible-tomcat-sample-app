---

# Build, deploy and test our Apache Tomcat sample image on localhost
- hosts: webservers
  force_handlers: true  # Delete the tempdir even if the image build fails
  roles:
  - role: python_deps_install
    tags: ['python_deps_install', 'python_deps', 'deps']
  - role: build_image
    tags: ['build_image', 'build']
  - role: start_container
    tags: ['start_container', 'start']
